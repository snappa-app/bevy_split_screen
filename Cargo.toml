[package]
name = "bevy_split_screen"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.1", default-features = false, features = ["bevy_camera", "bevy_window"] }

[dev-dependencies]
bevy = "0.19.1"
